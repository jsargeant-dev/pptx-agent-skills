interface:
  display_name: "PPTX Design Reference"
  short_description: "Map example compositions to planned slides"
  default_prompt: "Use $pptx-design-reference to select relevant supplied or bundled examples and create a per-slide reference map for $pptx-design."
