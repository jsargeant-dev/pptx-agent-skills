interface:
  display_name: "PPTX Design"
  short_description: "Build reference-first editable HTML slides"
  default_prompt: "Use $pptx-design to turn this intake and reference map into a varied, branded, editable 16:9 HTML slide deck and validate it before review."
